---
description: Show project status and recent activity
allowed-tools: Bash, Read
---

Show current project status:

1. Run `git status` for working tree state
2. Run `git log --oneline -5` for recent commits
3. Read last entries from .claude/CHANGELOG.md
4. Summarize any uncommitted work

Format as a quick status report.
